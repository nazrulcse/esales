<div class="j-menu-container"></div>
<div class="top-header-bg">
  <div class="container public_bg_image">
    <div class="b-inner-page-header f-inner-page-header">
      <div class="b-inner-page-header__content">
        <h1 class="f-primary-l c-default"><%= t(:ticket_and_travels) %></h1>

        <div class="f-primary-l f-inner-page-header_title-add c-senary">
          <%= t(:ticket_and_travel_title) %>
        </div>
      </div>
    </div>

    <hr class="hr-line">
  </div>
</div>
<div class="b-breadcrumbs f-breadcrumbs container adjust-breadcrumbs-position">
  <ul>
    <li><i class="fa fa-home"></i><%= link_to t(:home), root_path %></li>
    <li><i class="fa fa-angle-right"></i><%= link_to t(:products), products_path %></li>
    <li><i class="fa fa-angle-right"></i><span><%= t(:detail) %></span></li>
  </ul>
</div>
<div class="l-main-container">
  <section class="b-infoblock">
    <div class="container public_bg_image">
      <div class="row">
        <%= render 'travel_filter' %>
        <div class="col-md-9 ">
          <div class="row">
            <div class="b-col-default-indent travels_items">
              <% @travels.each do |travel| %>
                  <%= render 'item', travel: travel %>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</div>


<script type="text/javascript">
    $(function () {
        $(window).scroll(function () {
            if ($(window).scrollTop() == $(document).height() - $(window).height()) {
                $.ajax({
                    url: "<%= more_travels_path(params: params) %>",
                    type: 'get',
                    dataType: 'script',
                    data: {offset: $('.travels_items .single_item').length},
                    error: function () {
                        alert("Something wrong");
                    }
                });
            }
        });
    })
</script>
